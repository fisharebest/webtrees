<?php

declare(strict_types=1);

use Fisharebest\Webtrees\I18N;

/**
 * @var array<mixed> $chart_options
 * @var array<mixed> $chart_colors
 * @var string       $chart_title
 * @var array<array> $data
 * @var string       $language
 */

$id   = 'google-chart-' . bin2hex(random_bytes(8));
$name = 'callback_' . bin2hex(random_bytes(12));
?>

<?php if (count($data) === 1) : ?>
    <?= I18N::translate('This information is not available.') ?>
<?php else : ?>
    <div id="<?= $id ?>" title="<?= $chart_title ?>"></div>
    <script>
    let <?= $name ?> = function () {
        let chartOptions = <?= json_encode($chart_options) ?>;
        chartOptions.colors = webtrees.colors.fromCss(<?= json_encode($chart_colors) ?>);
        
        statistics.drawColumnChart(
            <?= json_encode($id) ?>,
            <?= json_encode($data) ?>,
            chartOptions
        );
    };

    if (document.readyState === "complete"
        || (document.readyState !== "loading" && !document.documentElement.doScroll)
    ) {
        statistics.init(<?= json_encode($language) ?>);
        statistics.addCallback(<?= $name ?>);
    } else {
        document.addEventListener("DOMContentLoaded", function () {
            statistics.init(<?= json_encode($language) ?>);
            statistics.addCallback(<?= $name ?>);
        });
    }

    </script>
<?php endif; ?>
