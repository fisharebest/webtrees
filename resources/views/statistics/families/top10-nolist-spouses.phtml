<?php

declare(strict_types=1);

/**
 * @var array<array{family:Family,age:string}> $records
 */

use Fisharebest\Webtrees\Family;

?>

<?php foreach ($records as $record) : ?>
    <?php $family = $record['family']; ?>
    <a href="<?= e($family->url()) ?>"><?= $family->fullName() ?></a> (<?= $record['age'] ?>)
<?php endforeach; ?>
