<?php

use Fisharebest\Webtrees\I18N;

?>

<span class="badge badge-<?= $context ?? 'secondary' ?>">
        <?= I18N::number($count) ?>
    <?php if (($total ?? $count) > $count) : ?>
        / <?= I18N::number($total) ?>
    <?php endif ?>
</span>
