<?php

declare(strict_types=1);

use Fisharebest\Webtrees\I18N;

/**
 * @var string|null $context
 * @var int         $count
 * @var int|null    $total
 */

?>

<span class="badge bg-<?= $context ?? 'secondary' ?>">
        <?= I18N::number($count) ?>
    <?php if (($total ?? $count) > $count) : ?>
        / <?= I18N::number($total) ?>
    <?php endif ?>
</span>
